#include <stdio.h>

int main(){
    int n1 = 1;

    do{
        printf("Salve ");
        n1 = n1 + 1;
    }while(n1 <= 3);
}

// do-while e a mesma coisa od while porem o codigo ele é executado todo no inicio porem no final ele ver se vai ter que executar dnv ou nao em quanto o while so funciona se a condição for verdadeira no inicio esse executa pelo menos uma vez
